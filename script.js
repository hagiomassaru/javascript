var numero = 5;
var vez2 = 0;
for (var vez = 0; vez < numero; vez++) {
    console.log("executando for " + vez);
}
while (vez2 < 5) {
    console.log("executando while " + vez2);
    vez2++;
}
console.log("Acabou");
